{
  "name": "agent-v2",
  "version": "0.0.1",
  "description": "",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "start-dev": "NODE_ENV=dev electron . --debug",
    "serve": "node server",
    "serve-dev": "NODE_ENV=dev nodemon server",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "MIT",
  "dependencies": {
    "electron": "^4.0.0"
  },
  "devDependencies": {
    "devtron": "^1.4.0",
    "eslint": "^5.10.0",
    "eslint-config-airbnb-base": "^13.1.0",
    "eslint-plugin-import": "^2.14.0",
    "nodemon": "^1.18.9"
  },
  "nodemonConfig": {
    "ignore": ["render/*", "main/*", "node_modules/*", "assets/*"]
  }
}
